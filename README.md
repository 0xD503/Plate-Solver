# Plate-Solver
Plate (astronomic) solver for FGW

# The project currently is under development


# Dependencies:
* OpenCV

# Installation
  Type in the command line:
  <  chmod +x ./install.sh
  <  ./install.sh
